name = "karmanya-infotech"
main = "server.js"
compatibility_date = "2024-01-01"

[vars]
# Add non-sensitive variables here
NODE_ENV = "production"

# Note: MONGODB_URI should be added as a SECRET via the Cloudflare Dashboard or 'wrangler secret put MONGODB_URI'
